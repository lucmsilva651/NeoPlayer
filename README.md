# neoplayer

To install dependencies:

```bash
npm install
```

To install submodules:

```bash
git submodule init && git submodule update
```

Init ``codicons``:

```bash
npm run icons
```

To run:

```bash
npm start
```
